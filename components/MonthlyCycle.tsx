'use client';
import { useTranslation } from '../contexts/LanguageContext';
export default function MonthlyCycle() { const { t } = useTranslation(); return <section id="ciclo" className="bg-brand-black px-4 py-24 text-white lg:py-32"><div className="mx-auto max-w-7xl text-center"><p className="ds-section-badge inline-flex">{t.monthlyCycle.badge}</p><h2 className="mt-6 font-display text-4xl font-black uppercase italic md:text-6xl">{t.monthlyCycle.title}</h2><p className="mx-auto mt-6 max-w-3xl text-lg text-slate-300">{t.monthlyCycle.subtitle}</p><ol className="mt-12 grid gap-5 md:grid-cols-2 lg:grid-cols-4">{t.monthlyCycle.steps.map((step, index) => <li key={step.title} className="ds-card-shell rounded-3xl p-7 text-left"><p className="text-sm font-black text-brand-lime">{String(index + 1).padStart(2, '0')}</p><h3 className="mt-8 text-xl font-black uppercase">{step.title}</h3><p className="mt-4 text-sm leading-relaxed text-slate-300">{step.desc}</p></li>)}</ol><p className="mt-10 text-sm text-slate-400">{t.monthlyCycle.footerText}</p></div></section>; }
