'use client';
import { CircleAlert } from 'lucide-react';
import { useTranslation } from '../contexts/LanguageContext';
export default function ProblemContext() { const { t } = useTranslation(); const cards = Object.values(t.problem.cards); return <section id="problema" className="relative overflow-hidden bg-brand-black px-4 py-24 text-white lg:py-32"><div className="mx-auto grid max-w-7xl gap-12 lg:grid-cols-2 lg:items-center"><div><p className="ds-section-badge">{t.problem.label}</p><h2 className="mt-6 font-display text-4xl font-black uppercase italic leading-none md:text-6xl">{t.problem.title}</h2><p className="mt-6 max-w-2xl text-lg leading-relaxed text-slate-300">{t.problem.description}</p><ul className="mt-10 grid gap-4 sm:grid-cols-2">{t.problem.items.map((item) => <li key={item} className="ds-card-shell flex gap-3 rounded-2xl p-5 text-sm text-slate-200"><CircleAlert className="h-5 w-5 shrink-0 text-brand-lime" />{item}</li>)}</ul></div><div className="grid gap-4 sm:grid-cols-2">{cards.map((card, index) => <div key={card} className="ds-panel-shell rounded-3xl p-8"><p className="text-sm font-black text-brand-lime">{String(index + 1).padStart(2, '0')}</p><p className="mt-10 text-xl font-black uppercase">{card}</p></div>)}</div></div></section>; }
