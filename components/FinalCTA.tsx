'use client';
import Button from './Button';
import { useTranslation } from '../contexts/LanguageContext';
import { trackFunnelEvent } from '../lib/analytics';
export default function FinalCTA() { const { t, language, localeSignals, recommendedContactHref, recommendedContactRoute } = useTranslation(); const click = () => trackFunnelEvent('final_cta_click', { lang: language, section: 'final_cta', cta: 'understand_base_plan', country: localeSignals.countryBucket, route: recommendedContactRoute }); return <section id="contato" className="bg-brand-black px-4 py-24 text-white lg:py-32"><div className="mx-auto max-w-4xl rounded-[2rem] border border-brand-lime/25 bg-brand-lime/10 p-8 text-center md:p-14"><p className="ds-section-badge inline-flex">{t.cta.badge}</p><h2 className="mt-6 font-display text-4xl font-black uppercase italic leading-none md:text-6xl">{t.cta.title}</h2><p className="mx-auto mt-6 max-w-2xl text-lg leading-relaxed text-slate-200">{t.cta.desc}</p><div className="mt-9"><Button href={recommendedContactHref} target="_blank" rel="noopener noreferrer" onClick={click} size="lg">{t.cta.button}</Button></div><p className="mt-6 text-xs leading-relaxed text-slate-400">{t.cta.disclaimer}</p></div></section>; }
