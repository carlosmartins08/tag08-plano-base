'use client';
import { CalendarDays, FileText, Send, LineChart } from 'lucide-react';
import { useTranslation } from '../contexts/LanguageContext';
const icons = [CalendarDays, FileText, Send, LineChart];
export default function IncludedPillars() { const { t } = useTranslation(); return <section id="pilares" className="bg-brand-black px-4 py-24 text-white lg:py-32"><div className="mx-auto max-w-7xl text-center"><p className="ds-section-badge inline-flex">{t.pillars.badge}</p><h2 className="mt-6 font-display text-4xl font-black uppercase italic md:text-6xl">{t.pillars.title}</h2><p className="mx-auto mt-6 max-w-3xl text-lg text-slate-300">{t.pillars.subtitle}</p><div className="mt-12 grid gap-5 md:grid-cols-2 lg:grid-cols-4">{t.pillars.items.map((item, index) => { const Icon = icons[index]; return <article key={item.title} className="ds-card-shell rounded-3xl p-7 text-left"><Icon className="h-8 w-8 text-brand-lime" /><p className="mt-8 text-xs font-black text-brand-lime">{String(index + 1).padStart(2, '0')}</p><h3 className="mt-3 text-xl font-black uppercase">{item.title}</h3><p className="mt-4 text-sm leading-relaxed text-slate-300">{item.desc}</p></article>; })}</div></div></section>; }
