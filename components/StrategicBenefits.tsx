'use client';
import { useTranslation } from '../contexts/LanguageContext';
export default function StrategicBenefits() { const { t } = useTranslation(); return <section id="criterios" className="bg-brand-black px-4 py-24 text-white lg:py-32"><div className="mx-auto max-w-7xl"><p className="ds-section-badge">{t.strategicBenefits.badge}</p><h2 className="mt-6 font-display text-4xl font-black uppercase italic md:text-6xl">{t.strategicBenefits.title}</h2><p className="mt-6 max-w-3xl text-lg text-slate-300">{t.strategicBenefits.subtitle}</p><div className="mt-12 grid gap-5 md:grid-cols-2">{t.strategicBenefits.items.map((item, index) => <article key={item.title} className="ds-card-shell rounded-3xl p-7"><p className="text-xs font-black text-brand-lime">{String(index + 1).padStart(2, '0')}</p><h3 className="mt-5 text-xl font-black uppercase">{item.title}</h3><p className="mt-3 text-sm leading-relaxed text-slate-300">{item.desc}</p></article>)}</div></div></section>; }
